pipeline {
    agent any
    tools {
        jdk 'JDK 11'
    }
    stages {
        stage('Build') {
            steps {
                sh 'java -version'
            }
        }
    }
}
